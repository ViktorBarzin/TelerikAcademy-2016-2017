var numbers = [1,2,3,4]

// to remove
numbers.splice(start,count); -> returns removed elements

// to add
 numbers.splice(index,0,'elements','to','insert')

// to replace
numbers.splice(index,amountToRemove,'replacements')

